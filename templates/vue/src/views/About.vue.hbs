<template>
  <div class="page">
    <h2>关于</h2>
    <p>这是关于页面。</p>
    
    <h3>项目信息</h3>
    <div class="info-grid">
      <div class="info-item">
        <strong>框架:</strong> Vue {{vueVersion}}
      </div>
      {{#if typescript}}
      <div class="info-item">
        <strong>语言:</strong> TypeScript
      </div>
      {{/if}}
      {{#if router}}
      <div class="info-item">
        <strong>路由:</strong> Vue Router
      </div>
      {{/if}}
      <div class="info-item">
        <strong>构建工具:</strong> Webpack 5
      </div>
      <div class="info-item">
        <strong>代码检查:</strong> ESLint
      </div>
    </div>
    
    <h3>技术栈</h3>
    <p>这个项目使用了现代化的 Vue.js 技术栈，提供了良好的开发体验和性能表现。</p>
  </div>
</template>

<script{{#if typescript}} lang="ts"{{/if}}>
{{#if (eq vueVersion "3")}}
{{#if typescript}}
import { defineComponent } from 'vue'

export default defineComponent({
  name: 'About'
})
{{else}}
export default {
  name: 'About'
}
{{/if}}
{{else}}
export default {
  name: 'About'
}
{{/if}}
</script>

<style scoped>
h2 {
  color: #42b883;
  margin-bottom: 1rem;
}

.info-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 1rem;
  margin: 1rem 0;
}

.info-item {
  background: #f8f9fa;
  padding: 1rem;
  border-radius: 4px;
  border-left: 4px solid #42b883;
}

h3 {
  margin-top: 2rem;
  margin-bottom: 1rem;
  color: #2c3e50;
}
</style>