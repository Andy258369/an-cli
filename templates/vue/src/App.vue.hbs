<template>
  <div id="app">
    <header class="app-header">
      <h1>欢迎使用 An Frame CLI</h1>
      <p>这是一个基于 Vue {{vueVersion}} 的项目{{#if typescript}}，使用 TypeScript{{/if}}{{#if router}}，集成了 Vue Router{{/if}}</p>
      
      {{#if router}}
      <nav class="navigation">
        <router-link to="/">首页</router-link>
        <router-link to="/about">关于</router-link>
      </nav>
      {{/if}}
    </header>
    
    <main class="main-content">
      {{#if router}}
      <router-view />
      {{else}}
      <div class="page">
        <h2>Hello Vue {{vueVersion}}!</h2>
        <p>开始您的 Vue 开发之旅吧！</p>
        
        <div class="counter">
          <button @click="decrement">-</button>
          <span>计数: {{ count }}</span>
          <button @click="increment">+</button>
        </div>
        
        <h3>项目特性</h3>
        <ul>
          <li>Vue {{vueVersion}}</li>
          {{#if typescript}}<li>TypeScript 支持</li>{{/if}}
          <li>Webpack 5 构建</li>
          <li>ESLint 代码检查</li>
          <li>热重载开发</li>
        </ul>
      </div>
      {{/if}}
    </main>
  </div>
</template>

<script{{#if typescript}} lang="ts"{{/if}}>
{{#if router}}
{{#if (eq vueVersion "3")}}
{{#if typescript}}
import { defineComponent } from 'vue'

export default defineComponent({
  name: 'App'
})
{{else}}
export default {
  name: 'App'
}
{{/if}}
{{else}}
export default {
  name: 'App'
}
{{/if}}
{{else}}
{{#if (eq vueVersion "3")}}
{{#if typescript}}
import { defineComponent, ref } from 'vue'

export default defineComponent({
  name: 'App',
  setup() {
    const count = ref(0)
    
    const increment = () => {
      count.value++
    }
    
    const decrement = () => {
      count.value--
    }
    
    return {
      count,
      increment,
      decrement
    }
  }
})
{{else}}
import { ref } from 'vue'

export default {
  name: 'App',
  setup() {
    const count = ref(0)
    
    const increment = () => {
      count.value++
    }
    
    const decrement = () => {
      count.value--
    }
    
    return {
      count,
      increment,
      decrement
    }
  }
}
{{/if}}
{{else}}
export default {
  name: 'App',
  data() {
    return {
      count: 0
    }
  },
  methods: {
    increment() {
      this.count++
    },
    decrement() {
      this.count--
    }
  }
}
{{/if}}
{{/if}}
</script>

<style>
#app {
  min-height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.app-header {
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(10px);
  padding: 2rem;
  text-align: center;
  box-shadow: 0 2px 20px rgba(0, 0, 0, 0.1);
}

.app-header h1 {
  color: #2c3e50;
  margin-bottom: 0.5rem;
  font-size: 2.5rem;
}

.app-header p {
  color: #7f8c8d;
  font-size: 1.1rem;
}

.main-content {
  padding: 2rem;
  max-width: 1200px;
  margin: 0 auto;
}

h2 {
  color: #42b883;
  margin-bottom: 1rem;
}

ul {
  text-align: left;
  margin: 1rem 0;
}

li {
  margin: 0.5rem 0;
}
</style>